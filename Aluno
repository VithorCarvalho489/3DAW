<?php
$host = "localhost";
$user = "root";
$pass = "";
$banco = "escola";

$matricula = $_POST['matricula'];
$nome = $_POST['nome'];
$email = $_POST['email'];

$linha = "{$matricula};{$nome};{$email}" . PHP_EOL;

$arquivo = "aluno.txt";

$handle = fopen($arquivo, "a");

if ($handle) {
    fwrite($handle, $linha);
    fclose($handle);
    echo "Aluno cadastrado com sucesso no arquivo!";
} else {
    echo "Erro ao abrir o arquivo para cadastro.";
}
?>
